# Règles de contribution

- Utiliser des branches `feature/<nom-utilisateur>`.
- Messages de commit : `[type] : description concise` (types : feat, fix, docs, style, refactor, chore).
- Ouvrir une PR vers `dev`. Une autre personne doit reviewer et approuver.
- Avant fusion finale vers `main`, rebase pour garder l'historique propre.
